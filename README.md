# Graphwise Stack — AWS EC2 + KIND

**Maintainer:** Kent Stroker

A **Helm-on-KIND** deployment of the Ontotext / Graphwise PoolParty
ecosystem plus the GraphRAG chatbot suite, on a single AWS EC2
instance. Designed as a personal demo environment for Graphwise
field presales engineers; published MIT-licensed, AS-IS, no
warranty.

## What you get, in one breath

`terraform apply` provisions an EC2 host (Amazon Linux 2023 / Graviton
ARM64), bootstraps Docker + KIND + kubectl + helm via cloud-init,
and starts a single-node Kubernetes cluster. Two `helm install`
runs put PoolParty (Thesaurus + GraphSearch + Extractor), two
GraphDB instances, Elasticsearch, Keycloak with realm imports,
ADF, Semantic Workbench, GraphViews, RDF4J, UnifiedViews, the
GraphRAG chatbot + conversation API + components + n8n workflows,
and a Console landing page in the cluster — each with its own
public subdomain, Let's Encrypt cert, and Keycloak SSO wiring
where applicable. The cluster-bootstrap step also brings up the
**Kubernetes Dashboard, Prometheus, and Grafana** behind the same
ingress-nginx + LE pattern (basic-auth gated, dashboards.* /
prometheus.* / grafana.* subdomains).

> **Demo-grade.** Default passwords, single-replica services, no
> hardening. Don't put real customer data in it.

## Two ways to deploy

- **Done this before, want to move fast:** [**QUICKSTART.md**](QUICKSTART.md) — sequential 0-18 step list, every command marked `# laptop` or `# EC2`. Cross-links to detail when you need it.
- **First time, want context with each step:** the table below + [SETUP.md](SETUP.md) → [DEPLOY.md](DEPLOY.md). More narrative, more rationale.

If you're new to Kubernetes / Docker / KIND, also read [**HOWITWORKS.md**](HOWITWORKS.md) — plain-English explainer of the layered architecture, the request-routing path, and the cert-manager + Let's Encrypt flow.

## Zero to deployed — abbreviated

For each step, the linked doc has the full walkthrough.

| # | Step | Reference |
|---|---|---|
| 1 | Get your laptop ready: terminal, Homebrew/Chocolatey, AWS CLI, **two IAM users** (Terraform user with `AmazonEC2FullAccess` + Bedrock user with `bedrock:InvokeModel`), Terraform, EC2 key pair, **pre-allocated Elastic IP** (capture both Allocation ID + Public IPv4), DNS A-records (`<sub>.<base>` apex + `*.<sub>.<base>` wildcard) pointing at the EIP, Graphwise Maven creds + license files, **clone the repo to your laptop**. | [SETUP.md](SETUP.md) |
| 2 | `cd infra/terraform && cp terraform.tfvars.example terraform.tfvars` and edit the REQUIRED block (`region`, `base_domain`, `subdomain`, `key_pair_name`, `admin_cidr`, `availability_zone`, `existing_eip_allocation_id`). | [DEPLOY.md §1](DEPLOY.md#1-provision-the-ec2-instance) |
| 3 | `terraform init && terraform apply`. ~3 min for the cloud-init bootstrap to bring the KIND cluster up. | [DEPLOY.md §1](DEPLOY.md#1-provision-the-ec2-instance) |
| 3.5 | **Lock the AMI** — `terraform output -raw ami_id` then paste into `terraform.tfvars` as `ami_override = "ami-..."`, run `terraform plan` (must show "No changes"). One-time post-apply step that prevents AWS-published AMI refreshes from force-replacing your EC2. | [DEPLOY.md §1.5](DEPLOY.md#15-lock-the-ami-one-time-immediately-after-first-apply) |
| 4 | **Verify DNS** propagated to the pre-allocated EIP — `dig +short <sub>.<base> poolparty.<sub>.<base>`. Slow path (no pre-allocation): email Kent the EIP from `terraform output godaddy_dns_records`. | [DEPLOY.md §2](DEPLOY.md#2-verify-dns-or-add-records-slow-path) |
| 5 | SSH in (`ssh -i $GRAPHWISE_KEY ec2-user@<eip>`), drop your Maven creds (`~/.ontotext/maven-{user,pass}`) and license files via scp (`files/licenses/*.{key,license}`). | [DEPLOY.md §3](DEPLOY.md#3-connect-and-prepare-creds) |
| 6 | `LE_EMAIL=you@example.com ./scripts/cluster-bootstrap.sh` — installs ingress-nginx, cert-manager, CNPG, Keycloak operator, metrics-server, Kubernetes Dashboard, kube-prometheus-stack (Prometheus + Grafana + AlertManager). | [DEPLOY.md §4](DEPLOY.md#4-install-cluster-operators) |
| 7 | `./scripts/extract-poolparty-realm.sh` — pulls the PoolParty realm export out of the Keycloak image and substitutes Ontotext's `${...}` placeholders for client secrets and admin password. | [DEPLOY.md §5](DEPLOY.md#5-install-the-realm-import-json-for-poolparty) |
| 8 | `./scripts/install-licenses.sh` — turns the license files into K8s Secrets. | [DEPLOY.md §6](DEPLOY.md#6-install-the-license-secrets) |
| 9 | Edit `charts/graphwise-stack/values.yaml` on the EC2 host to fill in your **Bedrock access key** and **n8n license activation key**. (n8n encryption key is auto-generated by Terraform.) | [DEPLOY.md §7](DEPLOY.md#7-configure-graphrag-runtime-credentials) |
| 10 | `./scripts/reset-helm.sh --yes <subdomain>` — installs both Helm releases (umbrella + graphrag). ~10–15 min. **No Maven creds yet?** Use `--skip-graphrag` for the umbrella-only path (~7-10 min); rerun without the flag once you have the credentials. | [DEPLOY.md §8](DEPLOY.md#8-deploy-the-stack) |
| 11 | Open `https://<sub>.<base>/` in a browser — Console landing page links to every app with credentials inline. | [CONSOLE-GUIDE.md](CONSOLE-GUIDE.md) |

Day-2 essentials:

- **Polite EC2 stop:** `./scripts/cluster-stop.sh` quiesces app workloads then prints the `aws ec2 stop-instances` command. Or just stop from the AWS Console — apps tolerate hard stop via WAL recovery.
- **EC2 start:** `./scripts/cluster-resume.sh` brings the KIND cluster back up after a host reboot.
- **Wipe and reinstall:** `./scripts/reset-helm.sh --yes <sub>` (deletes every PVC; data loss). Add `--skip-graphrag` for the umbrella-only path.
- **Non-destructive chart edit:** `helm upgrade …` against either the `graphwise-stack` or `graphrag` release.
- **Never run unscoped `terraform apply` post-provision** — read [infra/README.md → Safety](infra/README.md#safety-never-unscoped-terraform-apply-after-first-provision) before any `apply` after the initial one.
- **TLS certs default to `letsencrypt-staging`** (rate-limit-free for iteration; browsers warn "Not Secure"). Flip to real browser-trusted certs right before a customer demo: `./scripts/switch-cert-issuer.sh prod`. Flip back to staging post-demo: `./scripts/switch-cert-issuer.sh staging`. See [CLAUDE.md → Cert issuer toggle](CLAUDE.md) for rationale.

## Repo layout

```
charts/                  Helm charts (umbrella + per-app + vendored GraphRAG)
infra/kind/              Single-node KIND cluster definition
infra/terraform/         AWS module: EC2, SG, EIP association, cloud-init bootstrap
scripts/                 cluster-bootstrap, cluster-resume, cluster-stop,
                         render-values, reset-helm, install-licenses,
                         extract-poolparty-realm
scripts/laptop/          push-to-ec2, pull-from-ec2 helpers
files/licenses/          Vendor license files (gitignored)
QUICKSTART.md            Fast-path 0-18 sequential deploy
SETUP.md                 Laptop-zero prerequisites guide (macOS + Windows)
DEPLOY.md                End-to-end deploy walkthrough + day-2 lifecycle
HOWITWORKS.md            Plain-English explainer of the layered architecture
CONSOLE-GUIDE.md         URLs, credentials, smoke tests, troubleshooting
CLAUDE.md                Architecture deep dive, invariants, debugging notes
```

## Companion docs

- **[QUICKSTART.md](QUICKSTART.md)** — fast-path 0-18 step deploy for operators who've done this before. Sequential, every command marked `# laptop` or `# EC2`, cross-links to detail.
- **[SETUP.md](SETUP.md)** — laptop-zero prerequisites guide (macOS + Windows): terminal, Homebrew/Chocolatey, AWS CLI, IAM (two-user actor model), Bedrock, Terraform, EC2 key pair, EIP pre-allocation + DNS, EC2 Instance Connect note, Graphwise creds. Stops where Terraform begins.
- **[DEPLOY.md](DEPLOY.md)** — end-to-end deploy walkthrough including `terraform.tfvars` field-by-field reference, AMI lock §1.5, post-apply runbook, day-2 lifecycle, architecture notes, troubleshooting. Every code block marked `# On laptop` or `# On EC2`.
- **[HOWITWORKS.md](HOWITWORKS.md)** — plain-English explainer for K8s/Docker/KIND-naive operators: container layering, the 5-hop request flow, per-app subdomain routing, the cert-manager + Let's Encrypt HTTP-01 dance, the Keycloak Ingress quirk, OIDC issuer-match invariant, day-2 persistence rules, symptom → first-command runbook.
- **[CONSOLE-GUIDE.md](CONSOLE-GUIDE.md)** — every URL, credential (user-facing logins + internal service-to-service secrets + user-supplied secrets), smoke-test command, and per-component troubleshooting flow.
- **[CLAUDE.md](CLAUDE.md)** — architecture deep dive: the Keycloak hostname rule, OIDC issuer-match invariant, two-release rationale, Ontotext realm-export placeholder substitution, GraphDB subchart fullname pattern, debugging history.
- **[infra/README.md](infra/README.md)** — Terraform module reference (variables, outputs, post-apply runbook, teardown, the all-important Safety section on never running unscoped `terraform apply` post-provision).
